# 05. 로드맵 — Phase 0~5

집중 개발 기준 약 2~3주. **달력 기간이 부담이면 세션 밀도로 압축한다** — 그래프에
중요한 건 기간이 아니라 **이벤트 수와 결정 밀도**다 (목표: 이벤트 80~120개, 결정 15개+).

각 페이즈는 **코드 산출물**과 **저장할 이벤트**를 함께 정의한다. 코드만 나오고
이벤트가 안 나온 페이즈는 완료가 아니다.

---

## Phase 0 — 셋업 (D0)

**코드/인프라**
- [ ] `git init`, GitHub 공개 저장소 생성
- [ ] README 첫 줄에 AiAkiv 공개 프로젝트 링크 (아직 비공개여도 자리는 잡아둔다)
- [ ] AiAkiv 공개 전용 팀/프로젝트 확인 — **AiAkiv-Roguelike / AiAkivRogueLike**
      (본 계정 `k2g`와 완전 분리) → [00_overview.md](00_overview.md)
- [ ] 프로젝트 페르소나 설정: *"결정에는 반드시 고려한 대안과 기각 이유를 함께 저장."*
- [ ] 동료 계정/별칭 확보 → [08_participants_workflow.md](08_participants_workflow.md)

**저장할 이벤트 (첫 기억)**
- `결정` — 저장 규약 확정 (`docs/06_memory_protocol.md`)
- `결정` — 스택 선정: FastAPI + 순수 Python 엔진, TS+Canvas 기각 (`docs/03_architecture.md`)
- `결정` — 사전 설계 전환점 교체: 실시간→턴제 폐기, 즉시판정→에너지로 대체
  (`docs/04_turn_system_pivot.md`)

---

## Phase 1 — 코어 루프 (D1~D3)

**코드 산출물**
- [ ] `engine/rng.py` — 시드 RNG. **`random` 전역 호출 금지 규칙 확립**
- [ ] `engine/dungeon.py` — 랜덤 방 배치 + L자 복도, 시드 결정론
- [ ] `engine/fov.py` — 반경 8 시야, visible/explored 분리
- [ ] `engine/turn.py` — **v1 즉시판정 (lockstep)** ★
- [ ] `engine/combat.py` — `dmg = max(1, atk - def)`
- [ ] `engine/ai.py` — Goblin 추격 AI (1종만)
- [ ] `app/main.py` + `static/index.html` — 텍스트 그리드, 이동/공격 가능
- [ ] `tests/test_dungeon.py` — 같은 시드 → 같은 맵
- [ ] **플레이 가능한 상태로 커밋** (이게 Phase 1의 합격선)

**저장할 이벤트**
- `결정` — 던전 생성 알고리즘 (랜덤 방 배치 채택 / BSP 기각)
- `결정` — **턴 처리 v1: 즉시판정 채택** ★ *(대안: 에너지 시스템 — 적 1종·속도
  없음에서 과설계. **적에 속도를 넣으면 재검토 필요**를 이벤트에 명시)* → DQ1
- `결정` — 엔진/프레임워크 분리 불변식 (engine은 fastapi를 import하지 않는다)
- `구현` — 각 모듈 완성 시 커밋 해시와 함께
- (버그가 나오면) `버그` — 규약대로 증상 + 근본 원인 + 수정 + 커밋

> **Phase 1을 건너뛰고 v2로 직행하지 말 것.** → [04_turn_system_pivot.md](04_turn_system_pivot.md) §5

---

## Phase 2 — 시스템 확장 (D4~D8) — 데모의 밀도가 여기서 나온다

### 2-a. 턴 시스템 전환 ★ (사전 설계된 유일한 통과 지점)

- [ ] 적 3종(Rat 150 / Goblin 100 / Golem 60) 추가 → **v1이 실제로 막히는 것을 확인**
- [ ] 대안 A(카운터 hack)를 잠깐 시도해도 좋다 — 그 시도 자체가 좋은 기록
- [ ] **에너지 스케줄러로 전환** (대안 A/B/C 비교 후 C 채택)
- [ ] `tests/test_turn.py` — 속도 비율대로 행동하는지 검증
- [ ] `events[]` 응답 배열 도입 (API 계약 변경)

**저장할 이벤트**
- `결정` — **턴 처리 v2: 에너지 스케줄러 전환** ★ (증상 / 대안 A·B·C와 기각 이유 /
  전환 비용 표 / 커밋 해시) → **DQ1의 답**
  - 엔티티 **"턴 시스템"** 을 v1 이벤트와 공유할 것

### 2-b. 인벤토리 + 세이브 포맷 (BQ3의 재료)

- [ ] `engine/items.py` — 포션/검/방패/스크롤, 슬롯 10
- [ ] `engine/serialize.py` — 직렬화 스키마. **엔티티 이름 "세이브 포맷" 으로 명명**
- [ ] 인벤토리 추가 → 세이브 포맷 변경 (필연)

**저장할 이벤트**
- `구현` — 인벤토리 (엔티티: `인벤토리`, **`세이브 포맷`**)
- `결정` — 세이브 포맷 스키마 v1 (엔티티: **`세이브 포맷`**, `전역 시드`)
  → 두 이벤트가 `세이브 포맷` 을 공유 = **BQ3 성립**

### 2-c. 적 AI — cross-AI 귀속 (IQ1의 재료)

**반드시 서로 다른 AI 세션에서 각각 저장한다.** 한 세션에서 몰아 저장하면 작성자가
전부 같아져 IQ1이 죽는다.

| 단계 | 담당 | 저장 이벤트 | 엔티티 |
|---|---|---|---|
| 설계 | **ChatGPT** | `결정` — 적 AI 상태머신 (idle/chase/flee), 대안·기각 이유 | `적 AI` |
| 구현 | **Claude Code** | `구현` — 코드 + 커밋 해시 (co-author 컨벤션) | `적 AI` |
| 리뷰 | **Gemini** | `리뷰` — 지적 사항과 반영 여부 | `적 AI` |

### 2-d. 동료 등판 (IQ2의 재료)

- [ ] 동료가 **자기 계정에서** 코드 리뷰 / 플레이테스트 피드백 저장
- [ ] 가능하면 동료가 직접 PR 1개 (세이브/로딩 쪽이면 IQ2와 정확히 맞는다)

---

## Phase 3 — 밸런스 사이클 (D9~D11) — DQ2

**각 단계를 반드시 별도 이벤트로 저장한다.** 뭉쳐 쓰면 시간축이 안 생긴다.

1. [ ] `밸런스` — 던전/적 수치 **v1** (`FLOOR_PARAMS` → [02_game_design.md](02_game_design.md))
2. [ ] `밸런스` — **동료 플레이테스트 결과** (동료 계정에서 저장, 시드 포함 → 재현 가능)
3. [ ] `결정` — "너무 어려움" 판단과 **일부 롤백** (무엇을 되돌렸고 왜)
4. [ ] `밸런스` — 수치 **v2** 와 근거

전부 엔티티 **`던전 생성`** 공유 → `temporal_flow`로 진화가 드러난다.

---

## Phase 4 — 세이브/로딩 + 버그 회상 시연 (D12~D14) — 승부처

**코드 산출물**
- [ ] `store.py` — dict → SQLite
- [ ] `/api/game/{id}/save`, `/load`
- [ ] permadeath (사망 시 세이브 삭제)
- [ ] `tests/test_serialize.py` — 저장 → 로드 → 상태 동일 (**`rng_state` 포함**)
- [ ] 리팩터링 1회

**저장 규약 (이게 핵심)**
- 이 페이즈에서 **나오는 모든 버그를 그때그때 "증상 + 근본 원인 링크"로 저장한다.**
- 원인이 최근 커밋이면 그것대로, 과거 설계 결정까지 거슬러 올라가면 그때
  **인과 도약이 저절로 드러난다** — 심은 게 아니라 발견된 것.

**시연 장면 자체를 기록하기 (BQ1·BQ2의 실물)**
과거 결정이 원인인 버그가 나오면, AI에게 실제로 시킨다:

```
ak 이 버그와 관련된 과거 결정 찾아줘
```

그래프가 과거 결정을 물어오는 **그 과정을 기록으로 남긴다.** 방문자가 재현할 수 있는
실사례가 된다.

> **버그를 심지 않으므로 어떤 버그가 시연 대상이 될지는 개발이 정한다.**
> 나오면 대박, 안 나오면 없는 대로 정직. 억지로 만들지 않는다.

---

## Phase 5 — 마감과 공개 (D15+)

- [ ] `회고` 이벤트 저장 — 무엇을 배웠나, v1 결정들을 다시 본다면
- [ ] **커뮤니티 분석 실행** — auto-tag 주제 묶음이 실데이터에서 어떻게 나오는지 확인
      (투어 8단계의 실물)
- [ ] **프로젝트 공개 전환** (Public read — BP-103 §G config 활성화)
- [ ] 전 인증 계정에서 MCP read 동작 확인
- [ ] 배포 — 플레이 URL / GitHub 저장소 / AiAkiv 공개 프로젝트 **3종 상호 링크**
- [ ] [01_demo_goals.md](01_demo_goals.md)의 DQ1~PQ를 **공개 그래프에 실제로 던져보고**
      답이 나오는지 검수. 안 나오는 쿼리가 있으면 그건 저장 규약을 어긴 지점이다.
- [ ] devlog 시리즈 — Phase당 1편, 각 글이 공개 그래프의 해당 지점으로 링크

---

## 페이즈별 이벤트 밀도 목표

| Phase | 결정 | 구현/버그/밸런스 | 누적 |
|---|---|---|---|
| 0 | 3 | 1 | ~4 |
| 1 | 3~4 | 8~12 | ~20 |
| 2 | 5~6 | 20~30 | ~55 |
| 3 | 1~2 | 8~10 | ~70 |
| 4 | 2~3 | 15~25 | ~95 |
| 5 | 1 | 5 | **~100** |

**결정 15개 이상, 이벤트 80~120개.** 이 표에 못 미치면 저장을 빼먹고 있는 것이다.
