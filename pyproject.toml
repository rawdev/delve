[tool.pytest.ini_options]
# Put the repository root on the import path so plain `pytest` can import engine/.
# Without this, `python -m pytest` works but `pytest` raises ModuleNotFoundError,
# contradicting the command documented in docs/10_running.md.
pythonpath = ["."]
testpaths = ["tests"]
