# Test-only dependencies; these are not installed in the deployment container.
#
# Keep these separate so the deployed runtime still contains only FastAPI and Uvicorn.
#
# httpx is required internally by FastAPI TestClient for real HTTP-boundary tests.
# Engine tests still run as pure Python without HTTP.

-r requirements.txt

pytest
httpx
